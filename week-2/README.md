Я отвечу как всемирно известный инструктор по программированию доктор философии в области компьютерных наук с премией ACM SIGCSE за выдающийся вклад в преподавание программирования

TL;DR: Вот задания на **неделю 2**: работа со структурами, методами и обработкой ошибок. Все задания рассчитаны на 15–30 минут в день. Выполняйте последовательно — каждое строится на предыдущем.

---

## 🎯 Цель недели 2
Научиться моделировать данные с помощью `struct`, добавлять поведение через **методы** и обрабатывать ошибки **по-идиоматически** (без паники и исключений).

---

## 📝 Задание 1: Создать структуру `User`

Создайте новый модуль:
```bash
mkdir user-validator
cd user-validator
go mod init user-validator
```

В файле `user.go` опишите структуру `User`:

```go
package main

// User представляет пользователя системы
type User struct {
	Name  string
	Email string
	Age   int
}
```

> 💡 Поля с заглавной буквы — **экспортируемые** (публичные). В Go это важно для доступа из других пакетов.

---

## 📝 Задание 2: Добавить метод валидации

В том же файле `user.go` добавьте метод `Validate`:

```go
import (
	"errors"
	"strings"
)

// Validate проверяет корректность данных пользователя
func (u User) Validate() error {
	if u.Name == "" {
		return errors.New("имя не может быть пустым")
	}
	if u.Age < 0 || u.Age > 150 {
		return errors.New("возраст должен быть от 0 до 150")
	}
	if !strings.Contains(u.Email, "@") {
		return errors.New("email должен содержать @")
	}
	return nil // нет ошибки → валидно
}
```

> 🔍 Обратите внимание:  
> - `(u User)` — это **receiver** (аналог `this` в JS, но по значению).  
> - Метод возвращает `error` — так Go сообщает об ошибках.

---

## 📝 Задание 3: Использовать в `main`

Создайте `main.go`:

```go
package main

import "fmt"

func main() {
	user1 := User{
		Name:  "Друг",
		Email: "drug@example.com",
		Age:   25,
	}

	err := user1.Validate()
	if err != nil {
		fmt.Println("Ошибка:", err)
	} else {
		fmt.Println("Пользователь валиден:", user1.Name)
	}

	// Попробуйте невалидного пользователя
	user2 := User{Name: "", Email: "bad", Age: -5}
	err = user2.Validate()
	if err != nil {
		fmt.Println("Ошибка:", err)
	}
}
```

Запустите:
```bash
go run .
```

Ожидаемый вывод:
```
Пользователь валиден: Друг
Ошибка: имя не может быть пустым
```

(или другая первая найденная ошибка)

---

## 🔍 Контрольные вопросы
Ответьте кратко (можно прямо здесь):
1. Почему receiver написан как `(u User)`, а не `(u *User)`? В чём разница?
2. Что вернёт `Validate()`, если все поля корректны?
3. Почему мы возвращаем `error`, а не бросаем исключение?
4. Можно ли вызвать метод `Validate` у переменной с нулевым значением (`var u User`)? Что будет?

---

## ⏳ Рекомендация по темпу
- День 1: создайте структуру и метод.
- День 2: протестируйте разные случаи в `main`.
- День 3: ответьте на контрольные вопросы.

Не стремитесь сделать всё за раз. Главное — понять **механику**: структура → метод → проверка ошибки.

---

Когда вы:
- запустите программу и увидите ожидаемый вывод,
- поймёте, как работает `Validate`,

→ пришлите:
1. Вывод вашей программы,
2. Ответы на контрольные вопросы,
3. Были ли сложности.

После этого напишите: **«Начинаю неделю 3»** — и мы перейдём к **интерфейсам и композиции**.

Удачи! Вы уже моделируете данные — это фундамент backend-логики.