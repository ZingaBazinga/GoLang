## 🎯 Цель недели 3
Понять, как Go достигает гибкости через маленькие интерфейсы и композицию, и уметь применять это на практике.

---

## 📚 Ключевая идея: «Accept interfaces, return structs»

- **В аргументах функций** — принимайте интерфейсы (для гибкости).
- **В возвращаемых значениях** — возвращайте конкретные структуры (для ясности).

Это позволяет писать тестируемый, расширяемый код без привязки к реализации.

---

## 📝 Задание 1: Определить интерфейс `Notifier`

Создайте новый модуль:
```bash
mkdir notifier-demo
cd notifier-demo
go mod init notifier-demo
```

В файле `notifier.go` определите интерфейс:

```go
package main

// Notifier описывает что-то, что может отправить уведомление
type Notifier interface {
	Send(message string) error
}
```

> 💡 Интерфейс **маленький** (всего один метод) — это идиоматично для Go.

---

## 📝 Задание 2: Реализовать два нотификатора

В том же файле добавьте две структуры и их методы:

```go
import (
	"errors"
	"fmt"
)

// EmailNotifier отправляет уведомления по email
type EmailNotifier struct {
	To string
}

func (e EmailNotifier) Send(message string) error {
	if e.To == "" {
		return errors.New("адрес email не указан")
	}
	fmt.Printf("[Email] Отправлено на %s: %s\n", e.To, message)
	return nil
}

// SlackNotifier отправляет уведомления в Slack
type SlackNotifier struct {
	Channel string
}

func (s SlackNotifier) Send(message string) error {
	if s.Channel == "" {
		return errors.New("канал Slack не указан")
	}
	fmt.Printf("[Slack] Отправлено в %s: %s\n", s.Channel, message)
	return nil
}
```

> 🔍 Обратите внимание:  
> - **Никакого `implements`!** Go автоматически считает, что `EmailNotifier` реализует `Notifier`, потому что у него есть метод `Send(string) error`.  
> - Это **неявная реализация** — один из ключевых принципов Go.

---

## 📝 Задание 3: Использовать интерфейс в функции

Добавьте в `notifier.go` функцию, которая **принимает интерфейс**:

```go
// NotifyUser отправляет приветствие через любой Notifier
func NotifyUser(n Notifier, name string) error {
	message := fmt.Sprintf("Привет, %s! Добро пожаловать.", name)
	return n.Send(message)
}
```

И добавьте `main`:

```go
func main() {
	email := EmailNotifier{To: "user@example.com"}
	slack := SlackNotifier{Channel: "#welcome"}

	// Используем один и тот же код для разных реализаций
	NotifyUser(email, "Друг")
	NotifyUser(slack, "Друг")

	// Попробуем сломанный нотификатор
	bad := EmailNotifier{To: ""}
	err := NotifyUser(bad, "Друг")
	if err != nil {
		fmt.Println("Ошибка:", err)
	}
}
```

Запустите:
```bash
go run .
```

Ожидаемый вывод:
```
[Email] Отправлено на user@example.com: Привет, Друг! Добро пожаловать.
[Slack] Отправлено в #welcome: Привет, Друг! Добро пожаловать.
Ошибка: адрес email не указан
```

---

## 🔍 Контрольные вопросы
Ответьте кратко:
1. Почему `EmailNotifier` считается реализацией `Notifier`, хотя мы нигде не писали `implements`?
2. Что произойдёт, если добавить в `Notifier` второй метод, например `Retry() error`, но не реализовать его в `EmailNotifier`?
3. Почему в Go предпочитают маленькие интерфейсы (1–2 метода)?
4. Можно ли создать переменную типа `Notifier`? Приведите пример.

---

## 💡 Подсказка на будущее
Интерфейсы особенно мощны при **тестировании**: вы можете подменить настоящий `EmailNotifier` на **мок**, который не отправляет письма, но проверяет, что метод вызван.

---

Когда вы:
- запустите программу и получите ожидаемый вывод,
- поймёте, как работает неявная реализация,

→ пришлите:
1. Вывод программы,
2. Ответы на контрольные вопросы.

После этого напишите: **«Начинаю неделю 4»** — и мы построим **настоящий HTTP-сервер** с JSON и маршрутами.

Вы уже пишете код в стиле настоящих Go-проектов! 🚀